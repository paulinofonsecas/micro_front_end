name: base_app
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  micro_core:
    path: packages/core/micro_core
  
  # MicroApps
  micro_app_counter:
    path: packages/micro_apps/micro_app_counter

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
